"use client";

import { Track } from "./createTracker";

export const TrackProvider = Track.Provider;
export const TrackPageView = Track.PageView;
export const TrackClick = Track.Click;
export const TrackImpression = Track.Impression;
