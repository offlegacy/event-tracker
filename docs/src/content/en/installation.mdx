# Installation

Event Tracker is a library designed to simplify event tracking in React applications. It supports versions `React 18.0.0` and higher.

To install the latest stable version, run the following command:

```shell npm2yarn
npm install @offlegacy/event-tracker
```
